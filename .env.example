# Production frontend API URL (set this in Netlify).
VITE_API_URL=https://shoppyglobe-fullstack-nmb6.onrender.com/api

# Optional local-development proxy target.
# Leave this unset to use the deployed Render backend automatically.
# Set it to http://localhost:5000 only when you are running the backend locally.
# VITE_DEV_API_PROXY_TARGET=http://localhost:5000
